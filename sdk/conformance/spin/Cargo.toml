[package]
name = "auqw-conformance-spin"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
description = "Conformance guest whose handle spins forever (fuel-trap evidence)"

[lib]
crate-type = ["cdylib", "rlib"]

# Guests need `unsafe` for the ABI pointer handoff; the workspace forbids
# unsafe_code, so this crate declares its own lints instead of inheriting.
[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
